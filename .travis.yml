language: python

sudo: false

python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.5-dev"
  - "3.6-dev"
  - "nightly" # currently 3.7-dev
  - "pypy"

env:
  - DJANGO_VERSION=1.10.3
  - DJANGO_VERSION=1.9.11
  - DJANGO_VERSION=1.8.16

install: pip install --upgrade --editable .[dev]

script: pytest
